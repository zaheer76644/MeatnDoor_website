fragment VariantDetails on ProductVariant {
	id
	name
	quantityAvailable
	pricing {
		price {
			gross {
				currency
				amount
			}
		}
	}
	attributes {
		attribute {
			id
			name
			slug
		}
		values {
			id
			name
			value
		}
	}
}
